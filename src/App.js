import { Button } from 'react-bootstrap';

function App() {
    return (
        <div className="App">
            <Button variant="primary">Hoge</Button>
        </div>
    );
}

export default App;
